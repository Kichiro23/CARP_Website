<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # SPA fallback for React Router (skip /api requests)
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteCond %{REQUEST_URI} !^/api
  RewriteRule . /index.html [L]
</IfModule>
